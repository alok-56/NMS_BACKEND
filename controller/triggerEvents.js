const { emitToAgent } = require("../utility/socket");

// monitoring ping to specific agent

// topology ping to specific agent

// file monitoring ping to specific agent

// command execution ping to specific agent
